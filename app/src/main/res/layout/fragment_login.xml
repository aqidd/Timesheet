<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="id.co.flipbox.magang.fragment.LoginActivityFragment">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_centerInParent="true"
        android:layout_height="wrap_content">
    <EditText
        android:id="@+id/loginEmailForm"
        android:hint="E-mail"
        android:layout_width="match_parent"
        android:inputType="textEmailAddress"
        android:textSize="14sp"
        android:layout_marginBottom="7dp"
        android:layout_height="wrap_content"/>

    <EditText
        android:id="@+id/loginPwdForm"
        android:inputType="textPassword"
        android:layout_below="@+id/loginEmailForm"
        android:hint="Password"
        android:layout_width="match_parent"
        android:textSize="14sp"
        android:layout_marginBottom="7dp"
        android:layout_height="wrap_content"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textColor="@color/headerText"
        android:background="@color/primary400"
        android:layout_below="@+id/loginPwdForm"
        android:text="LOG IN"
        android:id="@+id/loginButton" />
    </RelativeLayout>

</RelativeLayout>
